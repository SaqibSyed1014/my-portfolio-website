const BrandLines = () => {
    return (
        <section className="pb-28 pt-20 lg:pt-[50vh] px-6 grid lg:grid-cols-12 text-3xl relative z-10">
            <h2 className="lg:col-span-3 lg:col-start-2">
                I build digital platforms that keep the user connected.
            </h2>

            <p className="lg:col-span-5 lg:col-start-7 pt-12">
                In a digital world where the focus often lies on an efficient user experience, I want to add an extra layer of joy. I believe that is what leaves a lasting impression.
            </p>
        </section>
    )
}

export default BrandLines;
